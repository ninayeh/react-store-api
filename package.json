{
  "dependencies": {
    "json-server": "^0.15.1",
    "jsonwebtoken": "^9.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "start-old": "json-server --watch db.json --port 3003"
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
